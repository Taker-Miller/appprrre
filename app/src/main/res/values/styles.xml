<resources>
    <!-- Colores del tema -->
    <!-- Usamos Theme.Material3.DayNight.NoActionBar para compatibilidad con Material Components -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/greenPrimary</item>
        <item name="colorPrimaryDark">@color/greenDark</item>
        <item name="colorAccent">@color/yellowAccent</item>
        <item name="android:textColor">@color/black</item>
    </style>


    <style name="Widget.MaterialComponents.TextInputLayout.FilledBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">@color/white</item>
        <item name="boxStrokeColor">@color/greenPrimary</item>
        <item name="boxStrokeWidth">2dp</item>
    </style>
</resources>
